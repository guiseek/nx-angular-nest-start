import { accountFeatureApi } from './account-feature-api';

describe('accountFeatureApi', () => {
  it('should work', () => {
    expect(accountFeatureApi()).toEqual('account-feature-api');
  });
});
